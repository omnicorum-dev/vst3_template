# VST3 Template

This is a little template generator for the VST3 format.

Put all of the configuration of your plugin in config.txt
and run `python3 build_template.py`

This should construct all of the necessary files to
develop and build the VST3.
